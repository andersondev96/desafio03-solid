import { describe, expect, it } from 'vitest'

describe('Authentication (EC2)', () => {
  it('should be able to authenticate', async () => {
    const test = 'Authentication'

    expect(test).toEqual('Authentication')
  })
})
